# CLAUDE.md

本项目的开发指引统一维护在 **[`AGENTS.md`](AGENTS.md)**，请先读它。

那里包含：项目概览、技术栈、目录结构、**硬性约定（违反会出问题）**、
当前实现状态与测试覆盖情况、启动方式、演示账号、以及一份「踩过的坑」清单。

需要深入了解时，按 `AGENTS.md` 末尾的「文档索引」进入 `docs/` 下的专题文档。

**不要在本文件里重复 `AGENTS.md` 的内容** —— 两处维护必然发散。
本文件只做指向。
