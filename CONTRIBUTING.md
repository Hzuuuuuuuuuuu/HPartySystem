# 开发与协作规范

本规范适用于人类开发者和 AI 编码代理。开始任何代码、配置、SQL 或文档修改前，先阅读 [`AGENTS.md`](AGENTS.md) 和与改动有关的设计文档；具体代码写法以 [`docs/05-开发规范.md`](docs/05-开发规范.md) 为准。若文档与现有实现或其他文档冲突，先核对代码、DDL 和已应用的 Flyway 迁移，在同一 PR 中修正文档，不自行忽略冲突。

## 1. 分支和审核

- `main` 保存经审核的稳定版本；`dev` 是日常开发的集成分支。
- 每项工作都从最新 `origin/dev` 新建独立分支，建议使用 `feat/简短主题`、`fix/简短主题`、`docs/简短主题` 或 `chore/简短主题`。一个分支和 PR 只处理一个明确主题。
- 开发者和 AI 均不得直接向 `main` 或 `dev` 推送功能修改，也不得自行将工作分支合入它们。
- 完成开发和本地验证后，向 **`dev`** 提交 PR，由仓库管理员审核并合并。发布时由管理员另行审核 `dev` → `main` 的 PR。创建 PR 时必须检查目标分支，避免 GitHub 默认选中 `main`。
- 审核意见、检查失败或 `dev` 新增提交后，在工作分支修复、重新验证并更新 PR；管理员确认后方可合并。不要以“已创建 PR”代替完成审核。

从 `dev` 建分支的命令：

```bash
git fetch origin
git switch -c feat/short-topic origin/dev
```

如果本地已有同名分支，先检查其状态，不要覆盖它。并行任务尽量使用不同工作分支或独立 worktree；修改同一文件前协调归属，不覆盖别人的未提交改动。

## 2. 开发前核对文档

| 改动范围 | 必读文档 |
|---|---|
| 任何代码 | `AGENTS.md`、本规范、`docs/01-系统设计.md`、`docs/05-开发规范.md` |
| 发展党员流程 | `docs/02-入党流程25步定义.md` |
| 表、字段、索引、SQL | `docs/03-数据库设计.md` |
| 新增或调整接口、前端调用 | `docs/04-接口清单.md` |
| 扩展模块 | `docs/07-扩展模块说明.md` |
| 上线配置或发布 | `docs/06-生产上线清单.md` |

实现与文档必须一起更新。尤其要检查权限标识、数据范围、实体字段和菜单组件路径是否与文档一致。数据库变更只能新增下一个版本的 Flyway 迁移，不改已应用的迁移，不在共享库手工执行 `ALTER`。`sql/01-schema.sql` 和 `sql/02-init-system.sql` 是 V1/V2 的参考副本，不是部署入口；当前数据库结构以完整迁移链为准。

## 3. 与最新 `dev` 同步及解决冲突

**提交 PR 前必须同步最新 `origin/dev`，即使暂时没有可见冲突。** 本项目统一采用在工作分支合并 `dev` 的方式，避免改写已经共享的提交历史：

```bash
git fetch origin
git switch <工作分支>
git merge origin/dev
```

发生冲突时，在本地逐个理解并修改冲突文件；同时保留两侧需要的行为，不能只删除冲突标记。完成后执行 `git status`，确认没有 `unmerged paths`；`git ls-files -u` 必须无输出。随后 `git add <已解决文件>`，按 Git 提示完成合并提交，重新执行受影响的构建、测试和手工验证，再推送工作分支。**未解决冲突或验证失败时不能提交 PR。**

PR 审核期间如果 `dev` 又发生相关变化，按同样步骤再次同步并验证。不要对 `dev`、`main` 或多人共用的工作分支执行强制推送，不用 `reset --hard` 丢弃他人修改。若两项异步工作触及相同设计，除了消除文本冲突，还要检查行为、权限、迁移版本和测试是否仍然一致。

## 4. 提交和 PR 的最低要求

- 后端改动：运行受影响模块及其依赖的测试，例如 `mvn -f hparty-server/pom.xml -pl hparty-develop -am test`；修改发展党员规则时必须运行该命令。新增或修复行为要补充能覆盖该行为的测试。
- 前端改动：在 `hparty-web` 运行 `npm run build`，并手工检查受影响的页面和权限状态。
- 接口、权限、数据库变更：验证正常与越权场景、空数据和错误分支；新增 Flyway 迁移须在合适的非生产数据库上验证，不能通过改旧迁移“修复”。
- 仅文档改动：核对链接、命令、版本号及相互引用；无须假称代码测试已经运行。
- 提交前检查 `git status` 和差异，只提交本次工作所需文件。不得提交密钥、真实个人数据、生产配置、日志、构建产物或依赖目录。
- PR 写清目的、变更范围、所依据或更新的文档、测试命令及结果、人工验证、数据库迁移、风险和处理办法；未运行的检查要明确说明原因。界面变化附截图。使用仓库的 PR 模板。

管理员审核重点：需求与文档一致性、数据权限与越权防护、流程规则、Flyway 迁移和数据安全、测试证据、与最新 `dev` 的兼容性。发现阻塞问题先修改并复审，不以口头说明代替代码和文档修复。

## 5. GitHub 仓库设置（管理员执行）

文档本身不能阻止直接推送或绕过审核。管理员应为 `main` 和 `dev` 配置分支保护或规则集：禁止直接推送、要求 PR、限制合并权限并关闭不必要的绕过权限。若要强制由管理员审核，应由管理员填写 `CODEOWNERS` 并启用 Code Owner 审核要求；仅要求“一人批准”不能保证批准人是管理员。在 CI 建立后再把相应检查设为必需，并要求分支在合并前与目标分支保持最新。当前仓库没有自动化 CI，不能把本地验证当作已经存在的 GitHub 必需检查。
